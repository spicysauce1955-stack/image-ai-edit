# Required for the insertion pipeline
GEMINI_API_KEY=

# Optional — only needed if you pass --segment / advanced options
REPLICATE_API_TOKEN=

# Optional — IC-Light relight pass (currently disabled in UI; restyles objects)
FAL_KEY=

# Existing providers (unrelated to this pipeline, preserved from prior work)
MINIMAX_API_KEY=
ZHIPUAI_API_KEY=
